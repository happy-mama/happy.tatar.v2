import styled from "styled-components";

const root = {
  container: styled.div`
    display: flex;
    flex: 1;
    flex-direction: column;

    padding-left: 30px;
  `,

  info: styled.div`
    display: flex;
    flex-direction: column;

    margin-left: auto;
    margin-top: auto;

    padding: 30px;
  `,
};

export default root;
